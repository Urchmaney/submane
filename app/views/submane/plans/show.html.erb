<p style="color: green"><%= notice %></p>

<%= render @plan %>

<div>
  <%= link_to "Edit this plan", edit_plan_path(@plan) %> |
  <%= link_to "Back to plans", plans_path %>

  <%= button_to "Destroy this plan", @plan, method: :delete %>
</div>

<div>
  <% @subscribers.each do |subscriber| %>
    <div>
      <%= subscriber.name %>
    </div>
  <% end %>
</div>
